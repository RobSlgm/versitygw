apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "versitygw.fullname" . }}-test-connection"
  labels:
    {{- include "versitygw.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "versitygw.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
